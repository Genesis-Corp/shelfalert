import React from 'react';
import ReactDOM from 'react-dom/client';
import ShelfAlert from './ShelfAlert';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<ShelfAlert />);